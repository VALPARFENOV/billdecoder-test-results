#!/usr/bin/env python3
"""
Автоматический деплой презентации на GitHub Pages
"""

import os
import subprocess
import json
from pathlib import Path

def check_git_installed():
    """Проверяет, установлен ли Git"""
    try:
        subprocess.run(["git", "--version"], capture_output=True, check=True)
        return True
    except:
        return False

def init_git_repo():
    """Инициализирует Git репозиторий"""
    if not check_git_installed():
        print("❌ Git is not installed. Please install Git first.")
        return False
    
    # Проверяем, есть ли уже Git репозиторий
    if Path(".git").exists():
        print("✅ Git repository already exists")
        return True
    
    # Инициализируем новый репозиторий
    try:
        subprocess.run(["git", "init"], check=True)
        print("✅ Git repository initialized")
        return True
    except subprocess.CalledProcessError:
        print("❌ Failed to initialize Git repository")
        return False

def create_github_pages_structure():
    """Создает структуру для GitHub Pages"""
    
    # Создаем директорию docs (стандарт для GitHub Pages)
    docs_dir = Path("docs")
    docs_dir.mkdir(exist_ok=True)
    
    # Копируем файлы презентации
    import shutil
    
    files_to_copy = [
        ("test-results/web-presentation-english.html", "docs/index.html"),
        ("test-results/README_English_Presentation.md", "docs/README.md"),
        ("test-results/English_Presentation_Summary.md", "docs/SUMMARY.md")
    ]
    
    for src, dst in files_to_copy:
        if Path(src).exists():
            shutil.copy2(src, dst)
            print(f"✅ Copied: {src} → {dst}")
        else:
            print(f"⚠️  Not found: {src}")
    
    # Создаем .nojekyll файл (чтобы GitHub не обрабатывал как Jekyll)
    nojekyll_file = Path("docs/.nojekyll")
    nojekyll_file.touch()
    print("✅ Created .nojekyll file")
    
    return True

def create_deployment_instructions():
    """Создает инструкции по деплою"""
    
    instructions = """# 🚀 Deploy to GitHub Pages

## Quick Deploy (Recommended)

1. **Create GitHub Repository:**
   - Go to https://github.com/new
   - Repository name: `billdecoder-test-results`
   - Make it public
   - Don't initialize with README

2. **Push to GitHub:**
   ```bash
   git add .
   git commit -m "Add BillDecoder test results presentation"
   git branch -M main
   git remote add origin https://github.com/YOUR_USERNAME/billdecoder-test-results.git
   git push -u origin main
   ```

3. **Enable GitHub Pages:**
   - Go to repository Settings
   - Scroll to "Pages" section
   - Source: "Deploy from a branch"
   - Branch: "main" / "docs"
   - Save

4. **Access Your Presentation:**
   - URL: `https://YOUR_USERNAME.github.io/billdecoder-test-results`
   - Share this URL with colleagues!

## Alternative: Firebase Hosting

1. **Install Firebase CLI:**
   ```bash
   npm install -g firebase-tools
   ```

2. **Initialize Firebase:**
   ```bash
   cd docs
   firebase init hosting
   # Select docs folder as public directory
   ```

3. **Deploy:**
   ```bash
   firebase deploy
   ```

4. **Get URL:**
   - Firebase will provide a public URL
   - Share with colleagues!

## Manual Upload

If you prefer manual upload:
1. Zip the `docs` folder
2. Upload to any web hosting service
3. Share the URL

---
*Generated by BillDecoder testing system*
"""
    
    with open("DEPLOY_INSTRUCTIONS.md", 'w', encoding='utf-8') as f:
        f.write(instructions)
    
    print("✅ Created DEPLOY_INSTRUCTIONS.md")

def create_firebase_config():
    """Создает конфигурацию для Firebase"""
    
    firebase_config = {
        "hosting": {
            "public": "docs",
            "ignore": [
                "firebase.json",
                "**/.*",
                "**/node_modules/**"
            ],
            "rewrites": [
                {
                    "source": "**",
                    "destination": "/index.html"
                }
            ]
        }
    }
    
    with open("firebase.json", 'w', encoding='utf-8') as f:
        json.dump(firebase_config, f, indent=2)
    
    print("✅ Created firebase.json")

def main():
    """Основная функция"""
    print("🚀 Preparing BillDecoder presentation for GitHub Pages / Firebase")
    print("=" * 60)
    
    # Проверяем наличие файлов презентации
    if not Path("test-results/web-presentation-english.html").exists():
        print("❌ English presentation not found!")
        print("Run: python3 generate-english-presentation.py")
        return
    
    # Инициализируем Git
    if not init_git_repo():
        return
    
    # Создаем структуру для GitHub Pages
    if not create_github_pages_structure():
        return
    
    # Создаем конфигурацию Firebase
    create_firebase_config()
    
    # Создаем инструкции
    create_deployment_instructions()
    
    print("\n🎉 Ready for deployment!")
    print("\n📋 Next steps:")
    print("1. 📖 Read DEPLOY_INSTRUCTIONS.md for detailed steps")
    print("2. 🌐 Create GitHub repository")
    print("3. 📤 Push to GitHub")
    print("4. ⚙️  Enable GitHub Pages in repository settings")
    print("5. 🔗 Share the public URL with colleagues!")
    
    print(f"\n📁 Files ready in 'docs' folder:")
    print(f"   - index.html (main presentation)")
    print(f"   - README.md (instructions)")
    print(f"   - SUMMARY.md (summary)")
    print(f"   - .nojekyll (GitHub Pages config)")
    
    print(f"\n🔥 Firebase config created:")
    print(f"   - firebase.json (Firebase hosting config)")
    
    print(f"\n💡 Quick commands:")
    print(f"   git add .")
    print(f"   git commit -m 'Add BillDecoder test results'")
    print(f"   git push origin main")

if __name__ == "__main__":
    main()
